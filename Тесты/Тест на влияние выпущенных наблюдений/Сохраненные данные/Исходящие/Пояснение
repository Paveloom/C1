Файлы пронумерованы по следующему принципу:

Первый символ — номер эксперимента (различаются количеством выпущенных данных: в третьем — 25 значений, во втором — 5, а в первом — 0);
Второй символ — использовался ли массив исключений? (n — нет, y — да);
Третий символ — использовалось ли приведение к несмещённой оценке? (n — нет, y — да);
Четвёртый символ — номер выходных данных (в соответствии с выходными файлами программы: 1 — output1, 2 — output2, 3 — output3).
